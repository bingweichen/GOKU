import React from 'react';
import { Button } from 'antd';

const Example = () => {
  return (
    <div>
      <Button >123</Button>
    </div>
  );
};

Example.propTypes = {
};

export default Example;
